<% unless flash.empty? %>
  <script>
    <% flash.each do |type, msg| %>
    	<% color = 'light-blue lighten-3' if type == 'notice' %>
    	<% color = 'pink lighten-1' if type == 'alert' %>
    			M.toast({html: '<%= msg %>', classes: '<%= color %>'})
    		<% end %>
  </script>
<% end %>
