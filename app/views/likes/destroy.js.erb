$('#like-icon-comment-<%= @comment.id.to_s %>').
  html('<%= link_to "いいねを取り消す", shop_comment_likes_path(@comment), method: :post, remote: true, class: "like-btn hide-text" %>');
$('#like-text-comment-<%= @comment.id.to_s %>').
  html('<%= j render "comments/like_text", { likes: @comment.likes, comment: @comment } %>');
